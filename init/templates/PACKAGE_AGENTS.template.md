# 包级 / 目录级 AGENTS 说明

> 适用范围：当前目录及其子目录，除非被更深层 `AGENTS.md` 覆盖。

本文件用于沉淀当前包、当前业务域或当前模块的局部规则。这里只写与本目录强相关的长期规则，不重复根目录已有的通用约束。

## 1. 本目录职责

- 目录名称：`<目录路径>`
- 主要职责：<这个目录负责什么>
- 不负责内容：<明确哪些能力不应继续堆在这里>

## 2. 复用与依赖边界

- 优先复用入口：`<本目录已有封装 1>`
- 优先复用入口：`<本目录已有封装 2>`
- 对外依赖边界：<允许依赖哪些模块 / 不应直接依赖哪些模块>
- 对外暴露边界：<本目录对外提供哪些能力>

## 3. 编码约束

- 标识符、文件名、任务名遵循当前目录既有风格。
- 新逻辑优先放在与当前职责一致的文件，不把无关逻辑堆进本目录。
- 输入边界必须校验，异常不能静默吞错。
- 如果本目录包含副作用逻辑，尽量把可测试纯逻辑下沉到更合适的子模块。

## 4. 测试与验证

- 当前目录修改后，优先验证：<单元测试 / 集成测试 / 人工路径>
- 高风险点：<本目录最容易出问题的地方>
- 变更后需要同步检查：<配置 / SQL / 文档 / 资源 / 迁移 / 示例>

## 5. OpenSpec 衔接

- 涉及当前目录的功能新增、行为变更、主流程调整时，默认先走 OpenSpec。
- `apply` 完成后自动进入 `review`。
- `review` 完成后停止等待用户确认，再决定是否 `archive`。
- 如果归档结论只影响当前目录，优先沉淀到当前 `AGENTS.md`。
